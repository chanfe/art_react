import React from "react";
import { Container } from "semantic-ui-react"
import "./Title.scss";

const Title = (props) => {
  return(
    <Container>
        <h1>Art Reaction</h1>
    </Container>
  );
};

export default Title;
